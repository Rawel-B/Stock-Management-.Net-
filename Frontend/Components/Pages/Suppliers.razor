@page "/suppliers"
@attribute [Authorize]
@inject SupplierService SupplierService
@inject ISnackbar Snackbar

<PageTitle>Suppliers</PageTitle>

<MudStack Spacing="3">
    <div class="page-heading">
        <div class="page-heading-text">
            <MudText Typo="Typo.h4">Suppliers</MudText>
            <MudText Typo="Typo.body2" Color="Color.Secondary">Manage supplier contacts and product relationships.</MudText>
        </div>
    </div>

    <MudPaper Class="section-panel pa-4" Elevation="0">
        <MudText Typo="Typo.h6" Class="mb-3">@(_supplier.Id == 0 ? "Add supplier" : "Edit supplier")</MudText>
        <MudGrid>
            <MudItem xs="12" md="3"><MudTextField @bind-Value="_supplier.Name" Label="Name" Required="true" /></MudItem>
            <MudItem xs="12" md="3"><MudTextField @bind-Value="_supplier.Phone" Label="Phone" /></MudItem>
            <MudItem xs="12" md="4"><MudTextField @bind-Value="_supplier.Email" Label="Email" /></MudItem>
            <MudItem xs="12" md="2" Class="form-actions">
                <MudButton Variant="Variant.Filled" Color="Color.Primary" StartIcon="@Icons.Material.Filled.Save" OnClick="SaveSupplier">Save</MudButton>
            </MudItem>
        </MudGrid>
    </MudPaper>

    <MudPaper Class="table-shell" Elevation="0">
        <MudTable Items="_suppliers" Dense="true" Hover="true" Elevation="0">
            <HeaderContent>
                <MudTh>Name</MudTh>
                <MudTh>Phone</MudTh>
                <MudTh>Email</MudTh>
                <MudTh>Products</MudTh>
                <MudTh></MudTh>
            </HeaderContent>
            <RowTemplate>
                <MudTd DataLabel="Name">@context.Name</MudTd>
                <MudTd DataLabel="Phone">@context.Phone</MudTd>
                <MudTd DataLabel="Email">@context.Email</MudTd>
                <MudTd DataLabel="Products">@context.ProductCount</MudTd>
                <MudTd DataLabel="Actions">
                    <MudIconButton Icon="@Icons.Material.Filled.Edit" Size="Size.Small" OnClick="@(() => EditSupplier(context))" />
                    <MudIconButton Icon="@Icons.Material.Filled.Delete" Size="Size.Small" Color="Color.Error" OnClick="@(() => DeleteSupplier(context))" />
                </MudTd>
            </RowTemplate>
            <NoRecordsContent>
                <MudText Typo="Typo.body2" Color="Color.Secondary">No suppliers added.</MudText>
            </NoRecordsContent>
        </MudTable>
    </MudPaper>
</MudStack>

@code {
    private SupplierEditModel _supplier = new();
    private List<SupplierViewModel> _suppliers = [];

    protected override async Task OnInitializedAsync() => await LoadSuppliers();

    private async Task LoadSuppliers()
    {
        _suppliers = await SupplierService.GetSuppliersAsync();
    }

    private async Task SaveSupplier()
    {
        if (string.IsNullOrWhiteSpace(_supplier.Name))
        {
            Snackbar.Add("Supplier name is required.", Severity.Warning);
            return;
        }

        await SupplierService.SaveSupplierAsync(_supplier);
        _supplier = new SupplierEditModel();
        await LoadSuppliers();
        Snackbar.Add("Supplier saved.", Severity.Success);
    }

    private void EditSupplier(SupplierViewModel supplier)
    {
        _supplier = new SupplierEditModel
        {
            Id = supplier.Id,
            Name = supplier.Name,
            Phone = supplier.Phone,
            Email = supplier.Email
        };
    }

    private async Task DeleteSupplier(SupplierViewModel supplier)
    {
        await SupplierService.DeleteSupplierAsync(supplier.Id);
        await LoadSuppliers();
        Snackbar.Add("Supplier deleted.", Severity.Info);
    }
}
